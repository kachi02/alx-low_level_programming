#include "main.h"
#include <stdio.h>
/**
 * abs - function that comptutes the absolute value of an integer
 * @c: is the function that will be use for that argument of the function
 * Return: 0
 */
int _abs(int c)
{
	if (c > 0 || c == )
	{
		return (c);
	}
	else
		return (c * -1);
}
